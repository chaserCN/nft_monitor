# NFT Gift Monitor Bot

Telegram бот для моніторингу подарунків NFT на маркетплейсі Portals.

## Функції

- 🔍 Автоматичний моніторинг подарунків кожні N хвилин
- 🎁 Фільтрація по комбінаціях "подарунок + модель"
- 💰 Фільтрація по максимальній ціні
- 📸 Відправка фото нових подарунків у канал
- ⚙️ Керування через команди бота
- 📊 Статистика роботи

## Деплой на Railway

### 1. Підготовка

1. Fork цей репозиторій на GitHub
2. Створіть бота через @BotFather у Telegram і отримайте токен
3. Створіть канал у Telegram і додайте бота як адміністратора
4. Отримайте Chat ID каналу (використайте `get_chat_id.py`)

### 2. Налаштування на Railway

1. Зайдіть на [Railway](https://railway.app)
2. Створіть новий проєкт з GitHub репозиторію
3. Додайте змінні оточення (Environment Variables):

```
TELEGRAM_BOT_TOKEN=ваш_токен_від_BotFather
TELEGRAM_CHANNEL_ID=ваш_chat_id_каналу
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
PORTALS_AUTH_DATA=ваші_дані_аутентифікації
```

4. Railway автоматично виявить `requirements.txt` і встановить залежності
5. Встановіть Start Command: `python3 bot.py`

### 3. Отримання API credentials

**TELEGRAM_API_ID та TELEGRAM_API_HASH:**
1. Зайдіть на https://my.telegram.org
2. Авторизуйтесь
3. Перейдіть до "API development tools"
4. Створіть додаток і скопіюйте `api_id` та `api_hash`

**PORTALS_AUTH_DATA:**
Відкрийте DevTools у браузері → Application → Cookies → знайдіть `authData` на сайті portals.tg

## Команди бота

- `/start` - Початок роботи
- `/help` - Детальна довідка
- `/showall` - Показати всі доступні подарунки
- `/list` - Показати відстежувані пари
- `/add <подарунок>,<модель>` - Додати пару
- `/delete <подарунок>,<модель>` - Видалити пару
- `/setprice <сума>` - Встановити макс. ціну
- `/setinterval <хвилини>` - Встановити інтервал
- `/pause` - Призупинити моніторинг
- `/resume` - Відновити моніторинг
- `/stats` - Статистика
- `/image <назва> #<номер>` - Показати зображення

## Локальний запуск

```bash
# Встановити залежності
pip3 install -r requirements.txt

# Створити .env файл з вашими даними
# (див. приклад вище в розділі "Налаштування на Railway")

# Запустити бота
python3 bot.py

# Зареєструвати команди (один раз)
python3 setup_commands.py
```

## Структура проєкту

- `bot.py` - Основний файл бота
- `bot_config.py` - Керування конфігурацією
- `gift_searcher.py` - Пошук подарунків на маркетплейсі
- `portals_auth.py` - Автентифікація в Portals
- `setup_commands.py` - Реєстрація команд у Telegram
- `get_chat_id.py` - Допоміжний скрипт для отримання Chat ID

## Ліцензія

MIT
